import styled from 'styled-components'

export const Title = styled.input`
  border: none;
  background: none;

  width: 100%;
  display: block;

  font-size: 18px;
  line-height: 1.35;
  font-weight: bold;
  font-family: 'Roboto Mono', serif;

  &:focus {
    outline: 0;
  }
`
